{
  "name": "recettes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm run build --webpack",
    "dev": "cd app && npm run dev",
    "start": "cd app && npm start"
  }
}